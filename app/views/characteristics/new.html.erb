<%# View: Characteristics#new — Form to create a new characteristic %>
<% content_for :title, "New Characteristic – AssetID" %>
<div class="page-header"><h1 class="page-title">New Characteristic</h1></div>
<div class="card"><div class="card-body">
  <%= form_with model: @characteristic do |f| %>
    <%= render "form", f: f, characteristic: @characteristic %>
  <% end %>
</div></div>
